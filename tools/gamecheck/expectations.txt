# What each title reaches today, as a floor.
#
# These are NOT targets. They are a record of where the emulator got to on a
# twenty-thousand-frame headless run, written down so that losing ground is a
# test failure rather than something someone notices by eye three weeks later.
# A title that does better than its line is not a failure; a title that does
# worse is a regression.
#
# Raise a line when a title genuinely improves. Never lower one to make a run
# green - a lowered floor is a bug report deleted.
#
# Format:  name prefix | cels | distinct screens | busiest frame's lit pixels
#
# These numbers are for a TWENTY THOUSAND frame run. A shorter run will fail
# them, which is correct rather than a bug in the file: several titles take
# well over a minute of emulated time to reach anything.
#
# Run with:
#   gamecheck <bios> <library> --frames 20000 \
#             --expect tools/gamecheck/expectations.txt
#
# Status, honestly. "Matches reference" means the machine this was ported from
# does the same thing with the same dump, so the shortfall is not ours alone.
# Where a title has been compared frame-for-frame, the figure is how many of
# its pixels differ from the reference's on the same frame.
#
#   Killing Time          its title menu. PIXEL IDENTICAL to the reference.
#   Flashback             its menu. 11% differ, and that is the character
#                         standing at a different point in his idle animation.
#   Alone in the Dark     its animated intro. PIXEL IDENTICAL.
#   Alone in the Dark 2   PIXEL IDENTICAL.
#   PGA Tour 96           gameplay. Differs from the reference only in where
#                         the two machines are in the round.
#   Battle Chess          its publisher splash at twenty thousand frames, and
#                         its board given sixty thousand - both machines need
#                         that long. Not a stall.
#   BattleSport           BIOS shell, "Insert CD". MATCHES REFERENCE.
#   Road Rash             BIOS shell, "Insert CD". MATCHES REFERENCE.
#                         Narrowed as far as the disc allows: its volume
#                         header is structurally identical to a working one,
#                         its root directory is present and valid at the
#                         offset it declares, and its length is inside what a
#                         longer working disc uses. The OS reads five sectors
#                         of the header area where a working disc reads six,
#                         asks for the disc info, and ejects. That area holds
#                         the disc's signature block, and both this machine
#                         and the reference reject it identically - which
#                         points at the dump rather than at either emulator.
#                         Not proven, only narrowed.
#   Doom                  BIOS shell - the dump is bad, chdman rejects it too
#   Need for Speed        was not stuck - it waits at a menu for a button.
#                         With --press it reaches ten thousand cels and then
#                         stops on "NVRAM Full: Configuration not saved",
#                         which is a real fault: the BIOS formats the NVRAM
#                         correctly and only forty-one bytes of thirty-two
#                         thousand are in use, so "full" is wrong.
#   Wing Commander III    Was stalled after 262 cels and 4,333 sectors. Fixed
#                         by charging the real memory-cycle costs: now 304
#                         cels and 27,853 sectors, and ten times the distinct
#                         frames. It was waiting on timing.
#   Wing Commander III d2 will not open at all - suspect a bad dump
#
# Eleven of twelve draw. The one that does not is a bad file.

Alone in the Dark (USA)       | 1300000 | 1000 | 55000
Alone in the Dark 2           |    7500 | 2200 | 55000
Battle Chess                  |    4500 |  700 | 35000
BattleSport                   |  180000 |  800 | 65000
Doom                          |  180000 |  800 | 65000
Flashback                     |  100000 | 1800 | 60000
Killing Time                  |   55000 |  800 | 70000
PGA Tour 96                   |   65000 | 1900 | 70000
Road & Track Presents         |      30 |    3 |  9000
Wing Commander III            |     300 | 4000 | 45000
